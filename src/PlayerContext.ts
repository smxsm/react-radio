import { createContext } from 'react';

export const PlayerControlContext = createContext<any>(null);
export const PlayerStatusContext = createContext<any>(null);
export const PlayerNowPlayingContext = createContext<any>(null);
